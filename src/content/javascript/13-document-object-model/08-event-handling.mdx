---
title: Event Handling
published: true
---

Event handling is the process of responding to user interactions with a web page. It involves attaching event listeners to HTML elements to listen for specific events and then executing a function when the event occurs.

### 🔹 1. `click`

```js
const btn = document.getElementById("btn");

btn.addEventListener("click", function () {
    alert("Button clicked!");
});
```

---

### 🔹 2. `mouseover`

```js
const box = document.getElementById("box");

box.addEventListener("mouseover", function () {
    this.style.backgroundColor = "yellow";
});
```

---

### 🔹 3. `mouseout`

```js
const box = document.getElementById("box");

box.addEventListener("mouseout", function () {
    this.style.backgroundColor = "white";
});
```

---

### 🔹 4. `keydown`

```js
document.addEventListener("keydown", function (event) {
    console.log("Key pressed: " + event.key);
});
```

> `document` is used directly here because it listens globally for any key press.

---

### 🔹 5. `keyup`

```js
document.addEventListener("keyup", function (event) {
    console.log("Key released: " + event.key);
});
```

---

### 🔹 6. `change`

```js
const inputBox = document.getElementById("inputBox");

inputBox.addEventListener("change", function () {
    console.log("Changed to: " + this.value);
});
```

---

### 🔹 7. `submit`

```js
const myForm = document.getElementById("myForm");

myForm.addEventListener("submit", function (e) {
    e.preventDefault(); // prevent page reload
    alert("Form submitted!");
});
```

---

### 🔹 8. `focus`

```js
const inputBox = document.getElementById("inputBox");

inputBox.addEventListener("focus", function () {
    this.style.borderColor = "blue";
});
```

---

### 🔹 9. `blur`

```js
const inputBox = document.getElementById("inputBox");

inputBox.addEventListener("blur", function () {
    this.style.borderColor = "gray";
});
```

---

### 🔹 10. `input`

```js
const inputBox = document.getElementById("inputBox");

inputBox.addEventListener("input", function () {
    console.log("Typing: " + this.value);
});
```

---

