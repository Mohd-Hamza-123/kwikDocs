---
title: Functions
description: ''
published: true
---

Functions are reusable blocks of code that perform a specific task. They help in organizing code and making it more readable. In Python, functions are defined using the `def` keyword followed by the function name and a pair of parentheses `( )`.

```python
def greet(name):
    """
    Function to greet a person.
    """
    return f"Hello, {name}!"

# Calling the function
message = greet("Hamza")
print(message)
```

#### 1) Returning Multiple Values

Python functions can return multiple values using tuples. For example:

```python
def get_user_info():
    name = "Hamza"
    age = 25
    return name, age  # Returns a tuple

# Unpacking the tuple
user_name, user_age = get_user_info()
print(user_name)  # Output: Hamza
print(user_age)   # Output: 25
```

#### 2) Default Arguments

You can provide default values for function arguments. If a value is not provided for an argument, the default value is used.

```python
def greet(name="Hamza"):
    return f"Hello, {name}!"

print(greet())      # Output: Hello, Hamza!
print(greet("Ali")) # Output: Hello, Ali!
```