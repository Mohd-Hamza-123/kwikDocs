---
title: Configuring MySQL
description: Configuring MySQL for Next.js
published: true
---

### Installing MySQL 

```bash
npm install --save mysql2
npm install --save-dev @types/node # if you are using TypeScript
```

### Creating a Database

Create a new file called `db.ts` in the `lib` folder. This file will contain the database connection logic.

```ts
import mysql from "mysql2/promise";
// Create a connection pool
const db_pool = mysql.createPool({
  host: 'localhost',
  user: 'root',
  database: 'test',
  password: 'password',
  waitForConnections: true,
  connectionLimit: 10,
  maxIdle: 10, // max idle connections, the default value is the same as `connectionLimit`
  idleTimeout: 60000, // idle connections timeout, in milliseconds, the default value 60000
  queueLimit: 0,
  enableKeepAlive: true,
  keepAliveInitialDelay: 0,
});
export default db_pool
```

