---
title: CSS Flexbox
description: Learn about the CSS Flexible Box Layout module (Flexbox) and its components for creating responsive layouts.
published: true
---

**What is CSS Flexbox?**

Flexbox is short for the Flexible Box Layout module. It is a layout method for arranging items in rows or columns.

---

### 🌟 CSS Flexbox Components

A flexbox always consists of:

- **Flex Container**: The parent (container).
- **Flex Items**: The items inside the container.

```html
<div class="flex-container">
  <div>1</div>
  <div>2</div>
  <div>3</div>
</div>
```

The flex container becomes flexible by setting the `display` property to `flex`:

```css
.flex-container {
  display: flex;
}
```

---

### 🚀 CSS Properties for Flex Containers

- **`flex-direction`**
- **`flex-wrap`**
- **`justify-content`**
- **`align-items`**

---

#### **1. Flex Direction**

This property specifies the display direction of flex items in the flex container.

```css
.flex-container {
  display: flex;
  flex-direction: row;
}
```

![Flex Direction](https://res.cloudinary.com/dkkifg17t/image/upload/v1737019819/Documentarium/JavaScript/k3hdyxnkz1klfkso6idy.jpg)

---

#### **2. Flex Wrap**

The `flex-wrap` property specifies whether the flex items should wrap or not.

```css
.flex-container {
  display: flex;
  flex-wrap: nowrap;
}
```

![Flex Wrap](https://res.cloudinary.com/dkkifg17t/image/upload/v1737019967/Documentarium/JavaScript/gsbcxnfqnx4uweepmlqs.png)

---

#### **3. Justify Content**

The `justify-content` property is used to align the flex items when they do not use all available space on the main axis (horizontally).

```css
.flex-container {
  display: flex;
  justify-content: center;
}
```

![Justify Content](https://res.cloudinary.com/dkkifg17t/image/upload/v1737020026/Documentarium/JavaScript/ggw40hzsm9j7s96ctabj.png)

---

#### **4. Align Items**

The `align-items` property is used to align the flex items when they do not use all available space on the cross axis (vertically).

```css
.flex-container {
  display: flex;
  height: 200px;
  align-items: center;
}
```
