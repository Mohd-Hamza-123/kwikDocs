---
title: CSS Selectors
description: Learn about different types of CSS selectors and how to use them to style HTML elements.
published: true
---

CSS selectors are used to "find" (or select) the HTML elements you want to style.
---

## Types of Selectors 🗂️

### 1️⃣ **Element Selector** 📄

The **element selector** targets all HTML elements of the same type.

#### Example:

```css
body {
  background-color: lightblue;
}

h1 {
  color: white;
  text-align: center;
}

p {
  font-family: verdana;
  font-size: 20px;
}
```

---

### 2️⃣ **ID Selector** 🔑

The **`id` selector** targets a single HTML element with a unique ID.  
**Note:** An ID name cannot start with a number or special symbols (`*`, `$`, `@`, etc.).

#### Example:

```css
#navbar {
  text-align: center;
  color: red;
}
```

```html
<nav id="navbar">This is the navbar</nav>
```

---

### 3️⃣ **Class Selector** 🏷️

The **`class` selector** is used to target one or more elements with the same class name.

#### Example:

```css
.main-header {
  text-align: center;
  color: red;
}
```

```html
<header class="main-header">This contains the header</header>
```

---

### 4️⃣ **Universal Selector ( \* )** 🌍

The **universal selector** applies styles to **all elements** on the page.

#### Example:

```css
* {
  text-align: center;
  color: blue;
}
```

---

### 5️⃣ **Grouping Selector** 👥

The **grouping selector** applies the same styles to multiple elements at once.

#### Example:

```css
h1, h2, p {
  text-align: center;
  color: red;
}
```

```html
<h1>Heading 1</h1>
<h2>Heading 2</h2>
<p>Paragraph</p>
```
---
