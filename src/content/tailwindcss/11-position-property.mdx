---
title : Position Property
published : true
---


### 🎯 **1. Position Types**

```html
<div class="static">Static (default)</div>
<div class="relative">Relative</div>
<div class="absolute">Absolute</div>
<div class="fixed">Fixed</div>
<div class="sticky">Sticky</div>
```

* **`static`** → default, not positioned
* **`relative`** → positioned relative to itself (used for child absolute elements)
* **`absolute`** → positioned relative to the nearest `relative` ancestor
* **`fixed`** → fixed relative to viewport (stays while scrolling)
* **`sticky`** → scrolls until a threshold, then sticks

---

### 🎯 **2. Offset utilities (top, right, bottom, left)**

Use `inset-*` or `top-*`/`right-*`/`bottom-*`/`left-*`.

```html
<div class="absolute top-0 left-0">Top left</div>
<div class="absolute bottom-0 right-0">Bottom right</div>
```

* `top-0`, `right-0`, `bottom-0`, `left-0` → stick to edges
* `top-4`, `left-10` → offsets using Tailwind spacing scale
* `inset-0` → set `top: 0; right: 0; bottom: 0; left: 0;`
* `inset-x-0` → left & right = 0
* `inset-y-0` → top & bottom = 0

👉 Custom values with arbitrary units:

```html
<div class="absolute top-[50px] left-[25%]">Custom offset</div>
```

---

### 🎯 **3. Z-index (stack order)**

```html
<div class="z-0">Default</div>
<div class="z-10">Above others</div>
<div class="z-50">Topmost</div>
```

👉 Custom z-index:

```html
<div class="z-[9999]">Custom z-index</div>
```

---

### 🎯 **4. Example: Centering with absolute**

```html
<div class="relative h-screen">
  <div class="absolute inset-0 flex items-center justify-center bg-blue-200">
    Centered Box
  </div>
</div>
```

✅ **Summary**

* Position → `static`, `relative`, `absolute`, `fixed`, `sticky`
* Offsets → `top-*`, `right-*`, `bottom-*`, `left-*`, `inset-*`
* Z-index → `z-0`, `z-10`, `z-50`, `z-[custom]`

---

