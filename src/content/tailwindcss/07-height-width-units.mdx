---
title : Height and Width Units
published : true
---


**Width & Height in Tailwind CSS**

### 1️⃣ **Pixel (px) values**

Tailwind has a **spacing scale** (like `1`, `2`, `4`, `8`, etc.) mapped to `px` values.

```html
<div class="w-10 h-10 bg-blue-400"></div> <!-- 40px x 40px -->
<div class="w-32 h-20 bg-green-400"></div> <!-- 128px x 80px -->
```

📌 But if you need **exact px**, you can use **arbitrary values**:

```html
<div class="w-[150px] h-[300px] bg-red-400"></div>
```

---

### 2️⃣ **Screen-relative sizes**

Tailwind provides utilities like:

```html
<div class="w-screen h-screen bg-purple-400"></div> <!-- full screen -->
<div class="w-full h-full bg-yellow-400"></div> <!-- full parent size -->
<div class="h-1/2 w-1/2 bg-gray-400"></div> <!-- half screen/parent -->
```

* `w-screen` / `h-screen` → 100vw / 100vh
* `w-full` / `h-full` → 100% of parent
* `h-1/2` / `w-1/2` → 50% of parent
* `h-1/3`, `h-2/3`, `h-1/4`, `h-3/4`, etc.

---

### 3️⃣ **Percentage (%)**

For custom percentages, use **fraction classes** or **arbitrary values**.

#### ✅ Fractions (predefined):

```html
<div class="w-1/2 h-1/3 bg-blue-500"></div> <!-- 50% x 33.3% -->
<div class="w-2/3 h-3/4 bg-green-500"></div> <!-- 66.6% x 75% -->
<div class="w-1/5 h-4/5 bg-red-500"></div>   <!-- 20% x 80% -->
```

#### ✅ Arbitrary values:

```html
<div class="w-[45%] h-[70%] bg-indigo-500"></div>
```

---

## ✅ **Summary**

* **Pixel (px):**

  * Use Tailwind scale → `w-10`, `h-32`
  * Or arbitrary → `w-[150px]`, `h-[300px]`
* **Screen-relative:**

  * `w-screen`, `h-screen`, `w-full`, `h-full`
* **Percentage (%):**

  * Fractions → `w-1/2`, `h-3/4`
  * Arbitrary → `w-[70%]`, `h-[45%]`

---

