---
title : Flexbox
published : true
---



### 🎯 **1. Enable Flexbox**

```html
<div class="flex">Flex container</div>
```

---

### 🎯 **2. Flex Direction**

```html
<div class="flex flex-row">Row (default)</div>
<div class="flex flex-row-reverse">Row reverse</div>
<div class="flex flex-col">Column</div>
<div class="flex flex-col-reverse">Column reverse</div>
```

---

### 🎯 **3. Flex Wrap**

```html
<div class="flex flex-wrap">Wrap items</div>
<div class="flex flex-wrap-reverse">Wrap reverse</div>
<div class="flex flex-nowrap">No wrap (default)</div>
```

---

### 🎯 **4. Justify Content (main axis alignment)**

```html
<div class="flex justify-start">Start</div>
<div class="flex justify-center">Center</div>
<div class="flex justify-end">End</div>
<div class="flex justify-between">Space between</div>
<div class="flex justify-around">Space around</div>
<div class="flex justify-evenly">Space evenly</div>
```

---

### 🎯 **5. Align Items (cross axis alignment)**

```html
<div class="flex items-start">Align top</div>
<div class="flex items-center">Align center</div>
<div class="flex items-end">Align bottom</div>
<div class="flex items-stretch">Stretch (default)</div>
<div class="flex items-baseline">Align by text baseline</div>
```

---

### 🎯 **6. Align Content (multi-line flex containers)**

(works only with wrapping `flex-wrap`)

```html
<div class="flex flex-wrap content-start">Content start</div>
<div class="flex flex-wrap content-center">Content center</div>
<div class="flex flex-wrap content-end">Content end</div>
<div class="flex flex-wrap content-between">Space between</div>
<div class="flex flex-wrap content-around">Space around</div>
<div class="flex flex-wrap content-evenly">Space evenly</div>
```

---

### 🎯 **7. Flex Grow / Shrink / Basis**

* **Grow** → how much item grows
* **Shrink** → how much item shrinks
* **Basis** → initial size before growing/shrinking

```html
<div class="flex">
  <div class="flex-none w-16 bg-blue-300">Fixed</div>
  <div class="flex-grow bg-green-300">Grow</div>
  <div class="flex-shrink bg-red-300">Shrink</div>
  <div class="basis-1/2 bg-yellow-300">50% width</div>
</div>
```

---

### 🎯 **8. Align Self (per item)**

```html
<div class="flex items-start h-32">
  <div class="self-auto">Auto</div>
  <div class="self-start">Top</div>
  <div class="self-center">Center</div>
  <div class="self-end">Bottom</div>
  <div class="self-stretch">Stretch</div>
</div>
```

✅ **Summary of Flexbox in Tailwind**

* **Container:** `flex`, `inline-flex`
* **Direction:** `flex-row`, `flex-col`, `flex-row-reverse`, `flex-col-reverse`
* **Wrap:** `flex-wrap`, `flex-nowrap`, `flex-wrap-reverse`
* **Justify Content:** `justify-*`
* **Align Items:** `items-*`
* **Align Content:** `content-*`
* **Flex Grow/Shrink/Basis:** `flex-grow`, `flex-shrink`, `basis-*`
* **Align Self:** `self-*`

---
