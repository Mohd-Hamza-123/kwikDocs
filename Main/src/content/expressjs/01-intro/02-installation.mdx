---
title : Installation
published : true
---


Create Express App with JavaScript

### 1. Create a New Project Folder

```bash
mkdir my-express-app
cd my-express-app
```

---

### 2. Initialize `package.json`

Run:

```bash
npm init
```

Now you’ll be prompted with questions. Example walkthrough:

```bash
package name: (my-express-app)  my-express-app
version: (1.0.0)  1.0.0
description: A simple Express.js app
entry point: (index.js)  index.js
test command:  (leave blank or type "jest" if you plan to use it)
git repository: (optional, leave blank if not using Git)
keywords: express, node, api
author: Your Name
license: (ISC)  MIT
```

👉 After this, `package.json` will be created.

---

### 3. Install Express

```bash
npm install express
```

---

### 4. Create the Entry File

Create `index.js` inside the project folder:

```js
// index.js
const express = require("express");
const app = express();

// Middleware
app.use(express.json());

// Route
app.get("/", (req, res) => {
  res.send("Hello, Express!");
});

// Start server
const PORT = 3000;
app.listen(PORT, () => {
  console.log(`🚀 Server running at http://localhost:${PORT}`);
});
```

---

### 5. Run the App

```bash
node index.js
```

Visit: [http://localhost:3000](http://localhost:3000)

---

### 6. (Optional) Setup Nodemon for Development

Install:

```bash
npm install --save-dev nodemon
```

Update `package.json` → `scripts` section:

```json
"scripts": {
  "start": "node index.js",
  "dev": "nodemon index.js"
}
```

Now run:

```bash
npm run dev
```

---


