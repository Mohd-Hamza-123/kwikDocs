---
title: Getting Form Values
published: true
---

In React, you can access form values using the FormData API (just like in vanilla JS). This is handy when you don’t want to make each field a controlled component.

### 1) use `onSubmit` event

```tsx
function App() {

  async function register(e) {
    e.preventDefault()
    const formData = new FormData(e.currentTarget)
    const username = formData.get("username");
    const email = formData.get("email");
    const password = formData.get("password");
    console.log(username)
    console.log(email)
    console.log(password)
  }

  return (
    <>
      <form onSubmit={register}>
        <input type="text" name="username" placeholder="Username" />
        <input type="email" name="email" placeholder="Email" />
        <input type="password" name="password" placeholder="Password" />
        <button type="submit">
          Register
        </button>
      </form>
    </>
  )
}

export default App
```

### 2) use `action` attribute 

```jsx
function App() {

  async function register(formData) {
    const username = formData.get("username");
    const email = formData.get("email");
    const password = formData.get("password");
    console.log(username)
    console.log(email)
    console.log(password)
  }

  return (
    <>
      <form action={register}>
        <input type="text" name="username" placeholder="Username" />
        <input type="email" name="email" placeholder="Email" />
        <input type="password" name="password" placeholder="Password" />
        <button type="submit">
          Register
        </button>
      </form>
    </>
  )
}

export default App
```

---

