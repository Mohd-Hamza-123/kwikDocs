---
title : Introduction to Tailwind CSS
published : true
---


Tailwind CSS is a **utility-first CSS framework** that gives you a big set of **ready-made CSS classes** (like `bg-blue-500`, `text-center`, `p-4`) so you can design your website **directly in your HTML/JSX** without writing separate CSS files.

It’s like having **LEGO blocks for CSS** – you combine small pieces (utilities) to build your design.


**Example**

#### 🔸 Without Tailwind (traditional CSS)

```css
.button {
  background: blue;
  color: white;
  padding: 10px 20px;
  border-radius: 8px;
}
```

```html
<button class="button">Click Me</button>
```

#### 🔸 With Tailwind

```html
<button class="bg-blue-500 text-white px-5 py-2 rounded-lg">
  Click Me
</button>
```

Here:

* `bg-blue-500` → background color blue
* `text-white` → text color white
* `px-5 py-2` → padding (x-axis = 20px, y-axis = 8px)
* `rounded-lg` → large rounded corners

---

### 🟢 **Why use Tailwind CSS?**

* 🚀 **Fast development** – no need to write custom CSS.
* 🎨 **Predefined design system** – consistent colors, fonts, spacing.
* 📱 **Responsive out of the box** – e.g. `md:text-lg` (only on medium screens).
* ⚡ **Optimized** – unused classes get removed, so your site is small & fast.
* 🔧 **Customizable** – change colors, fonts, and themes in `tailwind.config.js`.

---


