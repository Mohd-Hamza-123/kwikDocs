---
title: Server Actions in Components
published: true
---

Here’s a **minimal example** of using a **Server Action directly in a Server Component** with a simple form (`username`, `email`, `password`).

### Server Component

```jsx
// app/register/page.jsx (Server Component)
import { registerUser } from './src/actions/registerUser'
export default function RegisterPage() {
  return (
    <form action={registerUser}>
      <input type="text" name="username" placeholder="Username" required />
      <input type="email" name="email" placeholder="Email" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Register</button>
    </form>
  )
}
```

### Client Component

```jsx
// app/register/page.jsx (Server Component)
"use client"
import { registerUser } from './src/actions/registerUser'
export default function RegisterPage() {
  return (
    <form action={registerUser}>
      <input type="text" name="username" placeholder="Username" required />
      <input type="email" name="email" placeholder="Email" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Register</button>
    </form>
  )
}
```

### Server Action

```js
// src/actions/registerUser.ts
'use server'

export async function registerUser(formData: FormData) {
  const username = formData.get('username')
  const email = formData.get('email')
  const password = formData.get('password')

  // Example: insert into DB (replace with your logic)
  console.log({ username, email, password }) // server-only log
  // await db.user.create({ data: { username, email, passwordHash } })

  return { ok: true }
}
```


⚡ Key points:

* When we mark the `registerUser` function with `use server`, Next.js knows it should be executed on the server.
* The `<form>`’s `action={registerUser}` directly hooks into the **server action**.
* No `"use client"` is needed here because everything is handled in a **Server Component**.
* Validation and DB logic live securely in `actions.ts`.

---


